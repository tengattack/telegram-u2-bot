
const config = {
  accessCookie: '[u2 cookie]',
  botToken: '[bot token]',
  // check every 2 minutes
  checkInterval: 120000,
  allowChatIds: [ -1001025727615 ],
  db: {
    host: '127.0.0.1',
    port: 27017,
    // username: '',
    // password: '',
    db: 'u2',
  },
}

export default config
